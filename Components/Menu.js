import React from 'react'
import { View, Button, Text } from 'react-native'

class Menu extends React.Component {
  render() {
    return (
      <View>
        <Button title="Mode solo" onPress={() => {}}/>
        <Button title="Mode multijoueur" onPress={() => {}}/>
      </View>
    )
  }
}

export default Menu
